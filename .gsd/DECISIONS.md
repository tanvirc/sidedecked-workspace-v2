# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | arch | OAuth providers for MVP | Google + Discord | Google for mainstream, Discord for TCG community hub. Apple deferred (needs Developer Program). | Yes — add Apple when Developer Program enrolled |
| D002 | M001 | convention | Design system name | Voltage | Wireframes use "Voltage" naming. CSS tokens already use Voltage naming. PRD's "Midnight Forge" is superseded. | No |
| D003 | M001 | convention | Wireframe authority | HTML files in docs/plans/wireframes/ | HTML wireframes are the authoritative design source. Figma is the export target, not the source of truth. | Yes — if Figma workflow changes |
| D004 | M001 | arch | Cart optimizer approach | Greedy heuristic | Greedy is simplest, sufficient for MVP seller volumes (< 500). Revisit if optimization quality is insufficient at scale. | Yes — if conversion data shows suboptimal results |
| D005 | M001 | scope | Pixel-perfect priority pages | Card browse, card detail, deck builder, deck browser, deck viewer | User explicitly flagged these 5 pages as highest priority for visual fidelity. | No |
| D006 | M001 | convention | Wireframe coverage for generated pages | Happy path + key empty state | Generated wireframes cover primary content state and one empty state per page. Error states and loading states follow Voltage patterns generically. | Yes — if design review requests more states |
| D007 | M001 | arch | Milestone sequence | M001 (MVP) → M002 (Trust) → M003 (Growth) → M004 (Community) → M005 (Intelligence) | Risk-ordered: prove core loop first, then scale supply, then deepen engagement, then add intelligence. | Yes — if market feedback changes priorities |
| D008 | M001/S01 | convention | Price typography via CSS class | `.price` CSS class from globals.css | Single source of truth for tabular figures (`font-feature-settings: 'tnum'`, `font-family: var(--font-mono-stats)`, `color: var(--text-price)`). All PriceTag variants use this class instead of inline styles. | No |
| D009 | M001/S01 | convention | Voltage token application in components | Inline `style` for CSS custom properties | Tailwind doesn't have utility classes for custom Voltage tokens (--text-tertiary, --bg-surface-2, etc.). Components use inline `style={{ color: 'var(--text-tertiary)' }}`. Migrate if Tailwind is extended with Voltage utilities. | Yes — if Tailwind config extends Voltage tokens |
| D010 | M001/S01 | convention | footerLinks export shape | Named exports (navLinks, socialLinks) | Default export with 3 groups replaced by two named exports matching wireframe structure. Only Footer.tsx consumes this module. | No |
| D011 | M001/S02 | arch | Pagination over infinite scroll for browse/search | `usePagination` + `useHits` replacing `useInfiniteHits` | Wireframe specifies numbered pagination. Pagination gives URL-addressable pages (?page=2), better SEO, and matches user expectations for card catalogs. Infinite scroll kept only for mobile "load more" if needed. | Yes — if analytics show pagination hurts engagement |
| D012 | M001/S02 | arch | AlgoliaSearchResults refactored into CardSearchGrid | Extract shared sidebar+grid as `CardSearchGrid` | Both browse and search pages need the same sidebar filter + card grid layout. Extracting avoids duplicating Algolia widget mounting logic. The `InstantSearchNext` provider stays in each page's wrapper. | No |
| D013 | M001/S02 | convention | TrendingStrip with placeholder data | Component accepts `cards` prop, renders placeholder data in S02 | TrendingStrip needs live pricing data from customer-backend API (S04 scope). S02 creates the component shell with correct wireframe styling. S04 wires to real data. | No |
| D014 | M001/S02 | convention | Popular sets hardcoded per game | Static POPULAR_SETS data per game code | No API endpoint exists for "popular sets". Hardcode top 5-7 sets per game for MVP. Revisit when listing volume data makes dynamic ranking meaningful. | Yes — when sufficient listing data exists |
| D015 | M001/S02 | arch | Card detail mobile tabs dual-render | Desktop sections in `hidden md:block`, mobile tab-controlled content in `md:hidden` | Mobile wireframe has 4 tabs (Details/Prices/Sellers/Printings) while desktop shows all sections inline. Dual-render keeps components simple — no conditional logic mixing layout concerns. Trade-off: components mount twice in JSDOM tests (no CSS breakpoints), so tests use `getAllByTestId`. | No |
| D016 | M001/S02 | arch | SearchPageLayout owns its own InstantSearchNext provider | Same pattern as CardBrowsingPage | Each page (browse, search) composes its own chrome around shared CardSearchGrid. Search page adds breadcrumbs (Home > Search Results > "query") and a results header with hit count. Both pages own their InstantSearchNext provider for independent URL routing and initial state. | No |
| D017 | M001/S03 | convention | DeckSurface uses native tab bar instead of Radix Tabs | Native styled buttons matching viewer tab pattern | Radix Tabs was overkill for a two-tab toggle (Zones/List) and didn't match the wireframe's glassmorphic tab visual language used on the viewer page. Native buttons with inline styles match the established viewer tab pattern (brand-primary active border-bottom, uppercase text). | No |
| D018 | M001/S03 | convention | DeckZone collapse state is local per-zone | `useState` inside each DeckZone instance | Collapse/expand is a purely visual preference per zone — no cross-zone coordination needed. Avoids adding state to DeckBuilderContext for a UI-only concern. | Yes — if zone state needs persistence or coordination |
| D019 | M001/S03 | arch | DeckStatsPanel computes independently of DeckBuilderContext | Standalone computation from card array | Viewer page is read-only and should not import builder context. Stats (mana curve, type/color distributions) are computed from the deck's card array directly. ManaCurveChart is a reusable standalone component. | No |
| D020 | M001/S03 | convention | Game tabs replace game filter checkboxes for deck browsing | DeckGameTabs with underlined tab pattern | Wireframe uses tabs not checkboxes for game filtering on the browse page. Tabs use game-specific colors for active state (border-bottom + text). Different visual pattern from S02's GameSelectorStrip (grid layout). | No |
| D021 | M001/S05 | arch | Auth pages use separate (auth) route group | `(auth)` route group without Header/Footer | Wireframe auth pages are full-bleed split-screen without nav/footer. Using a separate route group (like existing `(checkout)`) avoids layout override hacks. Auth routes move from `/user` (login inline) + `/user/register` to `/login` + `/register`. `/user` redirects unauthenticated users to `/login`. | No |
| D022 | M001/S05 | convention | Profile tab state via URL hash | `window.location.hash` + `hashchange` listener | Tabs on the profile page use URL hash (`#collection`, `#settings`, etc.) so deep linking works. No client-only state — refreshing preserves the active tab. Lightweight, no router dependency. | Yes — if profile tabs need server-side rendering per tab |
| D023 | M001/S05 | process | Commit child repo changes to slice branch during task execution | `cd storefront && git add -A && git commit` on `gsd/M001/SXX` branch after each task | `storefront/` has its own `.git` — the parent repo's auto-commit only captures `.gsd/` artifacts, not storefront source files. Without explicit commits in the storefront repo during task execution, all source changes accumulate as uncommitted working tree modifications and are invisible to the parent's squash merge. Same applies to any other child repo with its own `.git` (backend/, customer-backend/, vendorpanel/, discord-bot/). | No |
| D024 | M001/S06 | convention | Wireframe generation grouped by layout family | Template-first batch generation: commerce → seller → user-account → misc | Grouping by shared layout (e.g., 8 user-account pages share sidebar nav, 5 seller pages share dashboard tabs) reduces effective unique work from 24 to ~6 layout variants with content swaps. Boilerplate (757 lines of tokens + canvas CSS) copied verbatim from existing wireframes to prevent token drift. | No |
| D025 | M001/S06 | convention | Figma export decoupled from wireframe generation | Separate task (T05) for Figma export, gated on user auth | Figma MCP requires OAuth which needs user action. Wireframe creation (R013) proceeds independently. Export (R025) is attempted last. If auth unavailable, HTML wireframes remain authoritative per D003. | Yes — if Figma MCP auth becomes automated |
| D026 | M001/S07 | arch | UserAccountLayout extracted in user/layout.tsx | 240px sidebar grid (desktop) + horizontal pill nav (mobile) | Wireframe shows 3-column `240px 1fr` grid with UserNavigation sidebar that collapses to horizontal pills on mobile. Implementing once in `user/layout.tsx` serves all 8 user-account pages. Verify-email opts out — renders standalone centered card per its wireframe. | No |
| D027 | M001/S07 | scope | S07 aligns list-card page wrapper only, not wizard | Page chrome + heading only | The list-card wireframe annotates the 3-step wizard as "S08 target design." S07 aligns only the page container, heading, and Voltage chrome. The wizard build is greenfield S08 work. | No |
| D028 | M001/S08 | arch | Wizard components in `seller/wizard/` subdirectory | `storefront/src/components/seller/wizard/` | Wizard has 13 files — too many for a flat `seller/` directory. Subdirectory keeps wizard-specific components isolated while preserving the `seller/` parent for shared seller components (ConditionGuide, ListingSuccessScreen, FeeCalculator). | No |
| D029 | M001/S08 | arch | Card search uses customer-backend catalog API, not Algolia | `GET /api/catalog/cards/search?q=...` | Wizard needs card ID + prints + set data for SKU construction. Catalog API returns this directly. Algolia is optimized for browsing with facets, not for identifying a specific card. Existing CardListingForm already uses this pattern. | No |
| D030 | M001/S08 | convention | Wizard state via useState, not URL-based steps | `useState` in ListingWizard parent | URL-based steps would cause state loss on browser back and add router complexity. The wizard is a single-page experience — all state lives in the parent component. Simpler and matches the wireframe's single-URL design. | No |
| D031 | M001/S09 | arch | Owned cards state uses useRef + version counter | `useRef<Set>` + `useState` version counter | A plain `useState<Set>` would cause re-render storms on every toggle (new Set reference). useRef holds the mutable Set; a version counter useState triggers re-renders only when the set actually changes. Callbacks read version to subscribe. | No |
| D032 | M001/S09 | arch | Override logic mutates optimizer result in-place | Post-optimization mutation | Re-running the full optimizer on every swap would be wasteful since only one card moves between seller groups. Instead, we remove the card from its current group, add it to the target group (or create one), and recalculate totals. Keeps swap instant. | No |
| D033 | M001/S09 | arch | Add-to-cart resolves catalogSku→variant via Medusa product lookup | `fetch(/store/products?catalog_sku=X)` | BackendListing doesn't contain Medusa variant IDs. The add-to-cart flow must resolve SKU→product→variant first, matching the existing CardDetailPage pattern. Sequential resolution prevents race conditions in cart updates. | No |
| D034 | M001/S09 | convention | Toast feedback for add-to-cart uses typed success/warning/error | `useToast()` from ToastProvider | Full success → success toast + close panel. Partial → warning toast with card/seller names. All failed → error toast. Matches existing app toast patterns. | No |
