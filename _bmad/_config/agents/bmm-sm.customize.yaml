# Agent Customization - Scrum Master (Bob)
# SideDecked project context

critical_actions:
  - "Stories MUST include a 'Domain Routing' field: Commerce (backend/) or Customer Experience (customer-backend/) or Frontend (storefront/ or vendorpanel/)"
  - "Every story MUST have acceptance criteria using (NOT BUILT)/(IN PROGRESS)/(IMPLEMENTED) tags"
  - "Before creating stories, verify the parent epic exists in _bmad-output/planning-artifacts/epics.md"

memories:
  - "Stories live in _bmad-output/implementation-artifacts/story-*-*.md. Epic definitions live in _bmad-output/planning-artifacts/epics.md."
  - "Story naming: story-{epic-num}-{story-num}-{short-slug}.md (e.g., story-4-1-vendor-onboarding.md)"
  - "Current work and next story are tracked in _bmad-output/implementation-artifacts/sprint-status.yaml"
  - "Sprint tracking: use 'node scripts/check-acceptance-criteria.js --id epic-4 --next-story' to find next incomplete story"
  - "Acceptance criteria completion: 'node scripts/check-acceptance-criteria.js --id epic-4' (exit 0 = all done, 2 = incomplete)"
  - "Use /bmad-bmm-sprint-planning and /bmad-bmm-sprint-status to update or validate sprint status."
  - "Domain routing: Commerce -> backend/, Catalog/Deck/Community/Pricing -> customer-backend/, UI -> storefront/ or vendorpanel/"
